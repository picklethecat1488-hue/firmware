[package]
name = "peripherals"
version = "0.1.0"
edition = "2021"

[features]
default = []
mock = []

[dependencies]
embedded-hal = "1.0.0"
model = { path = "../model" }

[dev-dependencies]
peripherals = { path = ".", features = ["mock"] }
